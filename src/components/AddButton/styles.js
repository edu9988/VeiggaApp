import { StyleSheet } from 'react-native'

export default StyleSheet.create({
  button: {
    position: 'absolute',
    zIndex: 9999,
    right: 20,
    bottom: 110,
    width: 80,
    height: 80,
    padding: 0,
    justifyContent: 'center',
    alignItems: 'center',
    backgroundColor: '#eee',
    borderRadius: 40,
    borderWidth: 1,
    borderStyle: 'solid',
    borderColor: '#aaa'
  }
})
